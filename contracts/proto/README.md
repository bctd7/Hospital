# RPC Contracts

本目录保存内部 RPC 的 `.proto` 契约。仅在出现明确的跨服务同步调用边界后增加契约。

不要创建没有调用方和数据边界的占位 RPC。新增 RPC 时遵循：

1. 定义消息和方法语义；
2. 使用 `goctl rpc protoc` 生成服务端和客户端；
3. 为超时、错误码和兼容性编写测试；
4. 不将数据库 Model 直接作为 RPC 消息。
